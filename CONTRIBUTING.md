# المساهمة في مختبر الأمن السيبراني

شكرًا لرغبتك في تحسين المشروع. الهدف هو نشر اختبارات دفاعية قابلة لإعادة التنفيذ من دون تعريض أشخاص أو شبكات حقيقية للخطر.

## قواعد أساسية

- اختبر فقط داخل نطاق تملكه أو لديك تصريح مكتوب لاختباره.
- لا تضف أسرارًا أو بيانات اعتماد أو عناوين أو إحداثيات أو ملفات التقاط حقيقية.
- لا تستخدم أسماء أشخاص أو مؤسسات حقيقية في الأمثلة.
- استخدم عناوين التوثيق مثل `192.0.2.0/24` و`198.51.100.0/24` و`203.0.113.0/24` عندما لا يحتاج المثال إلى شبكة محلية فعلية.
- اربط الأدوات بمصدرها الأصلي، وسجّل الإصدار أو الالتزام الذي اختُبرت عليه الخطوات.
- اجعل الجانب الدفاعي جزءًا أساسيًا: الكشف، الحماية، حدود الاختبار والتنظيف.
- لا تضف ملفات تنفيذية أو ثنائية أو سكربتات مبهمة المصدر.

## قالب الاختبار المقترح

يُفضّل أن يحتوي كل اختبار على الأقسام الآتية:

1. الهدف والنطاق المصرح به.
2. مستوى المخاطر والمتطلبات.
3. مخطط المختبر والعناوين التجريبية.
4. إصدارات الأدوات المختبرة.
5. خطوات التنفيذ وشرح الأوامر.
6. النتيجة المتوقعة ومعايير النجاح.
7. الأخطاء الشائعة واستكشافها.
8. وسائل الكشف والحماية.
9. خطوات الإيقاف واستعادة الحالة وحذف البيانات.
10. المراجع الأولية والرسمية.

## قبل فتح Pull Request

- اقرأ التغييرات كاملة وتأكد من أن الأوامر لا تستهدف شبكة إنتاج.
- تأكد من عدم وجود ملفات يلتقطها [`.gitignore`](.gitignore).
- شغّل فحص Markdown محليًا إن كان Node.js متاحًا:

```bash
npx --yes markdownlint-cli2 "**/*.md"
```

- تحقق يدويًا من الروابط والأوامر التي تغيرت.
- اشرح في Pull Request بيئة الاختبار والنتيجة الفعلية.

لن تُقبل مساهمة تعتمد على الخداع ضد مشارك غير موافق، أو تجمع بيانات تتجاوز هدف المختبر، أو تفتقر إلى خطوات تنظيف واضحة.
